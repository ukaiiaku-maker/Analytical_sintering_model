# Candidate 693168 final figure panel

The package contains 15 data-driven figure stems, each exported as vector PDF and 600-dpi PNG. It covers absolute trajectories, continuous physical time, pore stores and number proxies, closed accommodation, PR redistribution, migration channels, the full fine-​(T_2) Chen map, exact ablations, six-candidate context, bounded robustness, frozen fast-firing preservation, a Matplotlib mechanism schematic, and a compact final panel.

All figures label candidate 693168 as a **conditional Tier B** prototype. The dashboard and final panel explicitly state that it is not validation. The numerical plot-quality audit passes 37/37 checks.

The most important inspection sequence is:

1. `candidate_693168_final_panel` for the overview;
2. `candidate_693168_full_time_evolution` for absolute timing and grain growth;
3. `candidate_693168_pore_store_evolution` and `candidate_693168_closed_accommodation` for physical plausibility;
4. `candidate_693168_complete_Chen_filled_window` for lower/success/upper topology;
5. `candidate_693168_ablation_waterfall` for causality.
