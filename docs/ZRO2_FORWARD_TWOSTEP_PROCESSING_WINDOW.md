# Two-step processing window

The fixed ZrO2 model produces finite two-step windows at a 0.90 density target. The strongest Tier-like-B case is `P014`, with independently predicted `G1 = 37.82 nm`, a 96 h hold, and a 875–1000 °C success interval (125 °C width). One Tier-like-A window spans 875–950 °C. The smallest finite Tier-like-B `G1` is 37.82 nm; a diagnostic relaxed window appears at 35.99 nm.

These are nanoscale forward-prediction windows, but the long holds and low 0.90 density target limit interpretation. Lower T2 paths are density-exhaustion limited and higher T2 paths are growth limited. Every T2 schedule begins from an identical cloned first-step state and uses continuous physical time.

Candidate 693168 supplied only the desired response ordering, not ZrO2 state values. No fitting or optimization was performed. This is not validation.
