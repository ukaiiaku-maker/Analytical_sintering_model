# Relaxed TSS target diagnostics

The strict 0.976-density/0.29 µm classification remains unchanged: zero successes and zero finite windows. No successes occur at 0.5 µm. At 1.0 µm, successes appear (8 at density 0.976) but no finite window is bracketed. At 2.0 µm, successes are common, but finite windows appear only for the additionally relaxed density target 0.95 (8 windows). Every non-strict row is tagged `relaxed_diagnostic` and cannot overwrite strict results.

This indicates that strict grain suppression and densification jointly control failure; relaxation to 1–2 µm is diagnostic only. The common 950 °C state is controlled rather than method-fitted, barrier extrapolation remains uncertain, closed accommodation remains a proxy, microwave measurements are not thermal validation, and no validation claim is made.
