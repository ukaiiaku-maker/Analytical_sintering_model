# Resolved-rule ablation reinterpretation

This is not validation. The parent resolved-rule model has no finite Chen window. Consequently, none of the existing destructive ablations can establish destruction or preservation of a parent window. All rows are explicitly marked `not_interpretable_parent_has_no_window`; no ablation is described as destructive as expected.

The ablations may still show state sensitivity, but they are not causal window tests until a finite parent window exists.
