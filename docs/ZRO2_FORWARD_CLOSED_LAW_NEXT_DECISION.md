# Closed-law next decision

Retain `resolved_proxy_current` as the default. The comparison shows that dimensional GB/gas mappings can restore closed-pore removal, but their nominal candidate-state response is too abrupt and loses the lower boundary, while renewal-limited removal is too weak. None produces a finite strict naturally prepared Chen window.

The next useful evidence is independent closed-pore radius evolution, trapped-pressure information, accommodation kinetics, and a transport-length constraint. Those measurements can discriminate the GB, gas, and serial-renewal mappings without optimizing `Q_closed_app` or tuning unrelated mobility/barrier/diffusivity inputs. Candidate 693168 remains only a conditional qualitative comparator. No validation claim is made.
