# High-density topology-memory mechanism

The model separates conservative closure/detachment transfers from density-changing shrinkage. Open shrinkage acts only on open volume. Closed shrinkage acts only on closed volume and includes a gas-pressure drive. Connected-pore loss, persistent-junction state, multihit completion, pore drag, and stress memory enter migration. All local laws use instantaneous state and temperature, with no protocol labels.
