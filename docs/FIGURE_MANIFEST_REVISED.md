# Revised Figure Manifest After Observable-Effect Audit

The earlier manuscript package is retained for reproducibility but is
superseded for scientific interpretation by this ordering. In particular, a
positive internal `HR_pct > 1%` classification is not a paper-ready
fast-firing result.

## Main text

1. **Observable density--grain trajectories with ratio inset** --
   `rho_G_trajectories_representative`; common axes expose the negligible/weak
   separation and the 1.2, 1.5, and 2.0 thresholds.
2. **Practical Chen window map** -- existing `P6_chen_window_map`; retained as
   a kinetic-window result, without conflating it with a large trajectory
   effect.
3. **Fast-firing effect-size audit** -- `grain_size_ratio_vs_density`,
   `HR_TS_pct_vs_density`, and `density_window_effect_heatmap`; the narrative
   conclusion is insufficient observable separation.
4. **High-density support boundary** -- `high_density_attainment_map`; prevents
   extrapolation from the open-pore calculations to 0.95--0.99.

## Supplement

- `PR_memory_vs_visible_trajectory_effect`: connected fine pores, connected
  radius, PR work, and grain size on identical density coordinates.
- `negative_control_internal_vs_observable`: direct contrast between separated
  internal PR work and the sub-threshold grain-size trajectory ratio.
- Existing S2 and P10: q0/q1 and TJ-population ablations.
- Existing S4--S7: internal state histories, power/stress, censoring, and
  attainment diagnostics.

The source inventories remain useful, but any caption describing the current
fast-firing response as successful should be read in the former internal
classification sense only. The observable-effect audit is controlling for a
paper claim.
