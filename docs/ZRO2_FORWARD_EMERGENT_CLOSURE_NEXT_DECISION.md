# Next decision after the emergent closure final test

Do not activate `emergent_pore_closure_v1` in the accepted forward integrator. Its
final classification is `diagnostic_negative_result`.

The next work should address measurements and state mapping, not another abstract
rate fit:

1. Export bin-resolved closed inventory, pore number, radius, connectivity,
   shrinkability, gas inventory, and accommodation at each first-step clone point.
2. Determine why the naturally prepared inventory–availability product is orders of
   magnitude below the bracket needed for density attainment.
3. Derive the closed-pore sink geometry and (C_{GB,c}) from a consistent pore/GB
   geometry rather than selecting its magnitude from a successful curve.
4. Constrain accommodation consumption and surface-diffusion recovery independently.
5. Replace fractional gas pressure with trapped-mole and closure-temperature data.
6. Resolve the reconstructed energy-ledger violations using exact pore-bin area
   histories and specimen geometry.
7. Acquire or fit barrier data in the 850–1500 °C processing range.
8. Repeat the three fixed-state gate. A process map is justified only after a
   naturally prepared state shows a finite lower/success/upper interval and the
   energy ledger passes without hidden rescaling.

Do not introduce physical `Q_closed`, activate the failed mobility fit, or copy
candidate-693168 parameters. This decision is not validation. No validation claim
is made.
