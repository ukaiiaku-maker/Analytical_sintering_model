# Production Figure Manifest: Nucleation Fast/Chen

The generated inventory contains a dynamic classification map. A filled-window figure is intentionally absent because no finite complete window exists. Candidate assembly folders are also withheld because no common candidate passed.

The strict follow-up adds `strict_window_tier_map` and `Chen_window_fill_map_best_candidate`. These show the Tier B E0142 result; full-grid classification and representative triplet figures remain pending.
