# Mazaheri 8YSZ targets

The final target values and initial compact values are transcribed from the supplied task brief. They are not represented as PDF-extracted data. The required target PDF is absent, so all curve digitization files remain pending and no curve points are invented.
